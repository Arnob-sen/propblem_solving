#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define fastio                            \
    {                                     \
        ios_base::sync_with_stdio(false); \
        cin.tie(NULL);                    \
    }

// #define inf 1e12
int main()
{
	ll k;
	cin>>k;
	string s="";
	vector<ll>v;
	for(ll i=1;i<=k;i++)s+=to_string(i);
	
	cout<<s[k-1]<<endl;

		}
	
	
